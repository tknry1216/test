services:
  app:
    build: .
    image: servicechargeservice:latest
    container_name: servicechargeservice
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    restart: unless-stopped

services:
  app:
    build: .
    image: servicechargeservice:latest
    container_name: servicechargeservice
    env_file:
      - env.sample
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    restart: unless-stopped


